declare global {
  enum Gender {
    MALE = "male",
    FEMALE = "female",
    OTHER = "other"
  }

  enum StoryType {
    TEXT = 'text',
    IMAGE = 'image',
    VIDEO = 'video',
  }

  enum MediaType {
    IMAGE = 'image',
    VIDEO = 'video',
  }
}

export {};