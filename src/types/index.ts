export enum Gender {
  MALE = "male",
  FEMALE = "female",
  OTHER = "other"
}

export enum StoryType {
  TEXT = 'text',
  IMAGE = 'image',
  VIDEO = 'video',
}

export enum MediaType {
  IMAGE = 'image',
  VIDEO = 'video',
}